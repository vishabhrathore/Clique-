import { View } from "react-native";
import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context";

const OnBoarding = () => {
  return (
    <SafeAreaView>
      <View>
        <Text> new onboarding screen </Text>
      </View>
    </SafeAreaView>
  );
};

export default OnBoarding;
